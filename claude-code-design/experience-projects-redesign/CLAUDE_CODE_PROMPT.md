# Paste this to Claude Code

You're working in my personal-portfolio repo **`ariqmuldi/ariqmuldicom`**, branch **`ui-v2`** (Next.js 16 App Router, React 19, TypeScript; styling via CSS variables in `app/globals.css`).

**Read `design_handoff_experience_projects/README.md` in this repo top-to-bottom before writing any code — it is the full spec** (exact tokens, measurements, layout, prompt guidance, file list). Then implement the two chosen section redesigns it describes.

## What to build
1. **Experience** (`app/components/ExperienceSection.tsx` + `.gitlog__*` in `app/globals.css`) → design option **"2a"**: collapsed rows show a git **commit subject** + a green **`+N insertions · git show ▸`** diffstat (kill the clipped-summary truncation); clicking expands a full-width **`git show`** commit body.
2. **Projects** (`app/components/ProjectsSection.tsx` + `.proj-row*`) → design option **"3b"**: expandable rows — a terse tagline collapsed, expanding to a `cat README.md` panel + link out.

The design reference is `design_handoff_experience_projects/Experience Redesign.dc.html` (open it with `support.js` beside it). Implement ONLY option **"2a"** (Experience, turn 2) and **"3b"** (Projects, turn 3); ignore the other options in that file.

## Non-negotiables
- **UI only — do not change how content is sourced.** This site is data-driven: `app/data/experiences.ts` and `projects.ts` are auto-generated from `data/master-resume.tex` (never hand-edit or hardcode them); `app/data/role-content.json` is AI-generated. Keep every section rendering by `.map()`-ing its data. Do **not** hardcode any sample copy from the prototype — it must come from the data.
- **Reuse the existing design tokens** in `app/globals.css` `:root` and the existing class approach. No new fonts, colors, radii, or shadows (IBM Plex Mono, warm paper, 1px hairlines, no radius/shadow).
- **Experience expanded lists EVERY accomplishment bullet** for the role — the complete `accomplishments` array from `experiences.ts` (from the master résumé), the whole list, never a subset or cap.
- **Projects expanded breaks the description into its sentences** — each sentence (one accomplishment statement) on its own line as a markdown-style `- ` bullet, not one run-on paragraph. Split on sentence boundaries; every sentence must appear.

## Also extend the AI content pipeline (do NOT hardcode the new strings)
The new collapsed text must be **AI-generated exactly like the Work `description` already is**. `scripts/generate-role-content.ts` is the only network/`GEMINI_API_KEY` path and is manual (`npm run generate:content`) — keep it that way.
- Add a per-role **`commitSubject`** to `role-content.json` (imperative Conventional-Commits line, e.g. `feat(admissions): automate MDS review for 1,000+ applicants`), generated for every role in the same Gemini step + same `approved`/`sourceHash` approve-lock flow. ExperienceSection overlays it by `experienceId`, fallback `accomplishments[0]`. Roles with hidden/empty accomplishments (DOUBL) get no subject → show `● in active development`.
- Add a per-project **`tagline`** in a new `app/data/project-content.json` (terse GitHub-About one-liner, e.g. `Pomodoro timer fused with notes & to-do lists`), generated by summarizing each project's `description`, same approve-lock flow. ProjectsSection overlays it by `projectId`, fallback to the first sentence of `description`.

## When done
Run `npm run generate:content` (and the new project-content generation) so the fields populate, approve the drafts, then show me a diff of the changed components + CSS + scripts before committing. Don't commit `.env` or call the network anywhere except the manual generate step.
